@namespace("ade.audio")

protocol Synthesis {
    import idl "raw.avdl";

    record SynthesisRequest {
        string text;
        string? voice;
    }

    record SynthesisResponse {
        Audio audio;
    }
}
