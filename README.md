# anemone-data-exchange
A apache avro descriptor for anemone models data 

# List of tasks this will support is available [here](./tasks.md) (it may be subject to changes)


## Quicstart

Run `./run.sh` to compile and generate .avsc and .avpr

Use arguments `--python` and `--typescript` to generate also the object inthe right language